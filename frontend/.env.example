# URL da API (backend). Em desenvolvimento fica em branco (usa http://localhost:3001).
# Em produção, aponta para o URL do backend no Render, por exemplo:
#   VITE_API_URL=https://myvontade-api.onrender.com
VITE_API_URL=
